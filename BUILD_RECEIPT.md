# Build Receipt

## Verified

- Preserved baseline copy at `/tmp/evaluate-by-experiment.skill.baseline`.
- Added deterministic experiment-plan shape checker in `scripts/check_experiment_plan.py`.
- Added credible self-test in `scripts/self_test.py`.
- Extracted the fillable test-design template to `assets/experiment-plan-template.txt`.

## Unverified

- The static harness gate does not prove the quality of any particular experiment plan.
